import { useSelector } from "react-redux";

export const useMovies = () => useSelector(state =>{
    // return state.movies.movies.filter((movie) => movie.genre.includes((state.movies.filter.genre)));
})